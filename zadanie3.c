#include <stdio.h>
#include <ctype.h>
#include <string.h>

FILE *text1=fopen(1.txt,"r");
FILE *text2=fopen(copy,"r");
char *str[35];
fgets (str,sizeof(str),text1);
*p = str;

  while(*p)
    *p++ = toupper(*p);
  puts(str);
  
 fprintf(text2, str);
 return 0;
 }
  
